services:
  localclaw:
    image: openclaw:localclaw
    container_name: localclaw
    restart: unless-stopped
    ports:
      - "18901:18789"
    volumes:
      - ${HOME}/.openclaw/localclaw:/home/openclaw
